/*
* Copyright «©» 2016, J.R Totem. All rights reserved.
*
* This file was generated by MybatisModelGenerator v.3.0.
* All changes made in this file will be lost if you recompile the source schema.
*
* [GeneratorCrud v.1.0] by JRaffo 
*/

/**
* @author  Javier Raffo
* @version 1.0
* @since   17/03/2016 17:34:43
*/

Ext.define('MyApp.controller.GestionarSaldo.MainController', {
    extend: 'Ext.app.Controller',

    stores: ['GestionarSaldo.MainStore','GestionarGuia.GuiaPesoQueryStore'],

    models: ['GestionarSaldo.MainModel'],

    views: ['GestionarSaldo.Edit', 'GestionarSaldo.List'],

    refs: [
    	{
            ref: 'GestionarSaldoPanel',
            selector: 'panel'
        },
        {
            ref: 'GestionarSaldoList',
            selector: 'GestionarSaldoList'
        }
    ],

    init: function() {
    
        Ext.getStore('GestionarSaldo.MainStore').addListener('beforeload',this.setExtraParams,this);
        
        this.control({
            'GestionarSaldoList dataview': {
                itemdblclick: this.editRecord
            },
            'GestionarSaldoList button[action=add]': {
            	click: this.editRecord
            },
            'GestionarSaldoList button[action=delete]': {
                click: this.deleteRecord
            },
            'GestionarSaldoEdit button[action=save]': {
                click: this.updateRecord
            },
            'GestionarSaldoList button[action=filter]': {
                click: this.refreshGrid
            },
            'GestionarSaldoList button[action=clearFilter]': {
                click: this.resetForm
            },
            'GestionarSaldoList combobox': {
                change: this.refreshGrid
            },
            'GestionarSaldoList textfield': {
            	specialkey: this.onKeyEnter
            }
        });
    },

    editRecord: function(grid, record) {
        var edit = Ext.create('MyApp.view.GestionarSaldo.Edit').show();
        var form = edit.down('form');
        if(record.data){
        	edit.down('form').loadRecord(record);
        	edit.setTitle('Editar');
        	
        	var subModel_GuiaSaldoQuery = Ext.getCmp('subModel_GuiaSaldoQuery');
        	var id_guia = form.getForm().findField('id_guia');
        	
			subModel_GuiaSaldoQuery.store.reload({params: {id_guia: id_guia.getValue(), start:0, limit:10}});
			
        }else{
        	edit.setTitle('Agregar');
        	var subModel_GuiaSaldoQuery = Ext.getCmp('subModel_GuiaSaldoQuery');
			subModel_GuiaSaldoQuery.store.reload({params: {id_guia: -1, start:0, limit:10}});
        }
    },
    
    updateRecord: function(button) {
    	
        var win = button.up('window');
        var form = win.down('form');
        var record = form.getRecord();
        var values = form.getValues();
        var store = Ext.getStore('GestionarSaldo.MainStore');
        
        if (form.isValid()) {
	        Ext.Msg.confirm('Confirmar','¿Estás seguro de GRABAR el registro?',function (btn) {
	            
	            if (btn == 'yes') {
	        		if (values.id_guia > 0){
	        			values.tokenField = 'update';
	        			record.set(values);
	        		} else{
	        			record = Ext.create('MyApp.model.GestionarSaldo.MainModel');
	        			values.tokenField = 'insert';
	        			record.set(values);
	        			store.add(record);
	        		}
	                
	        		store.sync({
	        	        success: function (batch, eOpts) {
				        	store.load();
				        	win.close();
	        	        },
	        	        failure: function (batch, eOpts) {
							store.rejectChanges();
	        	        }
	        	    });
	            }
	        });
		}

    },
    
    deleteRecord: function(button) {
    	
    	var grid = this.getGestionarSaldoList();
    	var record = grid.getSelectionModel().getSelection(); 
    	var store = Ext.getStore('GestionarSaldo.MainStore');
    	
    	if(record.length > 0){
	        Ext.Msg.confirm('Confirmar','¿Estás seguro de ELIMINAR el registro?',function (btn) {
	                if (btn == 'yes') {
	             	   store.remove(record);
						store.sync({
					        success: function (batch, eOpts) {
					        	store.load();
					        }
					    });
	                }
	        });
    	}

    },
    refreshGrid: function() {
    	var store = Ext.getStore('GestionarSaldo.MainStore');
    	store.load({start:0});
    },
    resetForm: function() {
    	var store = Ext.getStore('GestionarSaldo.MainStore');
        var form = Ext.getCmp('form_filter_GestionarSaldo');
        form.reset();
    	store.load({start:0});
    },
    onKeyEnter: function(field, event, options) {
        if (event.getKey() == event.ENTER) {
        	var store = Ext.getStore('GestionarSaldo.MainStore');
        	store.load({start:0});
        }
    },
    setExtraParams: function(field, event, options) {
    	var store = Ext.getStore('GestionarSaldo.MainStore');
        var form = Ext.getCmp('form_filter_GestionarSaldo');
        var values = form.getValues();
    	store.getProxy().extraParams = values;
    }
    
});