/*
* Copyright «©» 2016, J.R Totem. All rights reserved.
*
* This file was generated by MybatisModelGenerator v.3.0.
* All changes made in this file will be lost if you recompile the source schema.
*
* [GeneratorCrud v.1.0] by JRaffo 
*/

/**
* @author  Javier Raffo
* @version 1.0
* @since   05/03/2016 15:14:59
*/

Ext.define('MyApp.controller.MttoSaldo.MainController', {
    extend: 'Ext.app.Controller',

    stores: ['MttoSaldo.MainStore'],

    models: ['MttoSaldo.MainModel'],

    views: ['MttoSaldo.Edit', 'MttoSaldo.List'],

    refs: [
    	{
            ref: 'MttoSaldoPanel',
            selector: 'panel'
        },
        {
            ref: 'MttoSaldoList',
            selector: 'MttoSaldoList'
        }
    ],

    init: function() {
    
        Ext.getStore('MttoSaldo.MainStore').addListener('beforeload',this.setExtraParams,this);
        
        this.control({
            'MttoSaldoList dataview': {
                itemdblclick: this.editRecord
            },
            'MttoSaldoList button[action=filter]': {
                click: this.refreshGrid
            },
            'MttoSaldoList button[action=clearFilter]': {
                click: this.resetForm
            },
            'MttoSaldoList combobox': {
                change: this.refreshGrid
            },
            'MttoSaldoList textfield': {
            	specialkey: this.onKeyEnter
            },
            'MttoSaldoEdit button[action=save]': {
                click: this.updateRecord
            }
        });
    },

    editRecord: function(grid, record) {
        var edit = Ext.create('MyApp.view.MttoSaldo.Edit').show();
        var form = edit.down('form');
        if(record.data){
        	edit.down('form').loadRecord(record);
        	edit.setTitle('Editar');
        	
//        	var subModel_GuiaPesoQuery = Ext.getCmp('subModel_GuiaPesoQuery');
        	var id_guia = form.getForm().findField('id_guia');
        	
        	var subModel_GuiaSaldoQuery = Ext.getCmp('subModel_GuiaSaldoQuery');
        	
			
        	
//			subModel_GuiaPesoQuery.store.reload({
//				params: {id_guia: id_guia.getValue(), start:0, limit:10},
//				scope: this,
//				callback: function(records, operation, success) {
//					if (success) {
//						//Obtenemos Totales
//						this.setGuiaTotales();
						
						subModel_GuiaSaldoQuery.store.reload({params: {id_guia: id_guia.getValue(), start:0, limit:10}});
//					}
//				}
//			});

			
        }
    },

    updateRecord: function(button) {
    	
        var win = button.up('window');
        var form = win.down('form');
        var record = form.getRecord();
        var values = form.getValues();
        var store = Ext.getStore('MttoSaldo.MainStore');
        
        if (form.isValid()) {
	        Ext.Msg.confirm('Confirmar','¿Estás seguro de GRABAR el registro?',function (btn) {
	            
	            if (btn == 'yes') {
	        		if (values.id_guia > 0){
	        			values.tokenField = 'update';
	        			record.set(values);
	        		} else{
	        			record = Ext.create('MyApp.model.MttoSaldo.MainModel');
	        			values.tokenField = 'insert';
	        			record.set(values);
	        			store.add(record);
	        		}
	                
	        		store.sync({
	        	        success: function (batch, eOpts) {
				        	store.load();
				        	win.close();
	        	        },
	        	        failure: function (batch, eOpts) {
							store.rejectChanges();
	        	        }
	        	    });
	            }
	        });
		}

    },
    
    refreshGrid: function() {
    	var store = Ext.getStore('MttoSaldo.MainStore');
    	store.load({start:0});
    },
    resetForm: function() {
    	var store = Ext.getStore('MttoSaldo.MainStore');
        var form = Ext.getCmp('form_filter_MttoSaldo');
        form.reset();
    	store.load({start:0});
    },
    onKeyEnter: function(field, event, options) {
        if (event.getKey() == event.ENTER) {
        	var store = Ext.getStore('MttoSaldo.MainStore');
        	store.load({start:0});
        }
    },
    setExtraParams: function(field, event, options) {
    	var store = Ext.getStore('MttoSaldo.MainStore');
        var form = Ext.getCmp('form_filter_MttoSaldo');
        var values = form.getValues();
    	store.getProxy().extraParams = values;
    },
    setGuiaTotales: function() {
    	Ext.Ajax.request({
            method: 'GET',
            url: '/InaviWSRestApp/api/GestionarGuia/guiaTotales',
            success: function( result, request ){
            	
            	var json = Ext.JSON.decode(result.responseText);
            	var formTotales = Ext.getCmp('form_totales');
            	
            	total_peso_bruto = formTotales.getForm().findField('total_peso_bruto');
            	total_peso_tara = formTotales.getForm().findField('total_peso_tara');
            	total_peso_devolucion = formTotales.getForm().findField('total_peso_devolucion');
            	total_peso_neto = formTotales.getForm().findField('total_peso_neto');

            	total_peso_bruto.setValue(json.record.total_peso_bruto);
            	total_peso_tara.setValue(json.record.total_peso_tara);
            	total_peso_devolucion.setValue(json.record.total_peso_devolucion);
            	total_peso_neto.setValue(json.record.total_peso_neto);
            }
        });
    }
    
});