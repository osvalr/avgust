/*
* Copyright «©» 2016, J.R Totem. All rights reserved.
*
* This file was generated by GeneratorCrud v.1.0.
* All changes made in this file will be lost if you recompile the source schema.
*
* [GeneratorCrud v.1.0] by JRaffo 
*/

/**
* @author  Javier Raffo
* @version 1.0
* @since   01/03/2016 22:42:35
*/

Ext.define('MyApp.controller.GestionarClienteJuridico.MainController', {
    extend: 'Ext.app.Controller',

    stores: ['GestionarClienteJuridico.MainStore'],

    models: ['GestionarClienteJuridico.MainModel'],

    views: ['GestionarClienteJuridico.Edit', 'GestionarClienteJuridico.List'],

    refs: [
    	{
            ref: 'GestionarClienteJuridicoPanel',
            selector: 'panel'
        },
        {
            ref: 'GestionarClienteJuridicoList',
            selector: 'GestionarClienteJuridicoList'
        }
    ],

    init: function() {
    
        Ext.getStore('GestionarClienteJuridico.MainStore').addListener('beforeload',this.setExtraParams,this);
    
        this.control({
            'GestionarClienteJuridicoList dataview': {
                itemdblclick: this.editRecord
            },
            'GestionarClienteJuridicoList button[action=add]': {
            	click: this.editRecord
            },
            'GestionarClienteJuridicoList button[action=delete]': {
                click: this.deleteRecord
            },
            'GestionarClienteJuridicoEdit button[action=save]': {
                click: this.updateRecord
            },
            'GestionarClienteJuridicoList button[action=filter]': {
                click: this.refreshGrid
            },
            'GestionarClienteJuridicoList button[action=clearFilter]': {
                click: this.resetForm
            },
            'GestionarClienteJuridicoList combobox': {
                change: this.refreshGrid
            },
            'GestionarClienteJuridicoList textfield': {
            	specialkey: this.onKeyEnter
            }
        });
    },

    editRecord: function(grid, record) {
        var edit = Ext.create('MyApp.view.GestionarClienteJuridico.Edit').show();
        var form = edit.down('form');
        
        if(record.data){
        	edit.down('form').loadRecord(record);
        	edit.setTitle('Editar');
        }else{
        	edit.setTitle('Agregar');
        	
          	var id_estado = form.getForm().findField('id_estado');
          	id_estado.setReadOnly(true);
          	id_estado.setValue(1);
          	
        }
    },
    
    updateRecord: function(button) {
    	
        var win = button.up('window');
        var form = win.down('form');
        var record = form.getRecord();
        var values = form.getValues();
        var store = Ext.getStore('GestionarClienteJuridico.MainStore');
        
        if (form.isValid()) {
	        Ext.Msg.confirm('Confirmar','¿Estás seguro de GRABAR el registro?',function (btn) {
	            
	            if (btn == 'yes') {
	        		if (values.id_persona > 0){
	        			values.tokenField = 'update';
	        			record.set(values);
	        		} else{
	        			record = Ext.create('MyApp.model.GestionarClienteJuridico.MainModel');
	        			values.tokenField = 'insert';
	        			record.set(values);
	        			store.add(record);
	        		}
	                	
	        		store.sync({
	        	        success: function (batch, eOpts) {
				        	store.load();
				        	win.close();
	        	        },
	        	        failure: function (batch, eOpts) {
							store.rejectChanges();
	        	        }
	        	    });
	            }
	        });
		}
    },
    
    deleteRecord: function(button) {
    	
    	var grid = this.getGestionarClienteJuridicoList();
    	var record = grid.getSelectionModel().getSelection(); 
    	var store = Ext.getStore('GestionarClienteJuridico.MainStore');
    	
    	if(record.length > 0){
	        Ext.Msg.confirm('Confirmar','¿Estás seguro de ELIMINAR el registro?',function (btn) {
	                if (btn == 'yes') {
	             	   store.remove(record);
						store.sync({
					        success: function (batch, eOpts) {
					        	store.load();
					        },
		        	        failure: function (batch, eOpts) {
								store.rejectChanges();
		        	        }
					    });
	                }
	        });
    	}

    },
    refreshGrid: function() {
    	var store = Ext.getStore('GestionarClienteJuridico.MainStore');
    	store.load({start:0});
    },
    resetForm: function() {
    	var store = Ext.getStore('GestionarClienteJuridico.MainStore');
        var form = Ext.getCmp('form_filter_GestionarClienteJuridico');
        form.reset();
    	store.load({start:0});
    },
    onKeyEnter: function(field, event, options) {
        if (event.getKey() == event.ENTER) {
        	var store = Ext.getStore('GestionarClienteJuridico.MainStore');
        	store.load({start:0});
        }
    },
    setExtraParams: function(field, event, options) {
    	var store = Ext.getStore('GestionarClienteJuridico.MainStore');
        var form = Ext.getCmp('form_filter_GestionarClienteJuridico');
        var values = form.getValues();
    	store.getProxy().extraParams = values;
    }
});