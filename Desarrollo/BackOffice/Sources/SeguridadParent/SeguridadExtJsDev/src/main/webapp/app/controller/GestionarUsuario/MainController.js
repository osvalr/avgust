/*
* Copyright «©» 2016, J.R Totem. All rights reserved.
*
* This file was generated by MybatisModelGenerator v.3.0.
* All changes made in this file will be lost if you recompile the source schema.
*
* [GeneratorCrud v.1.0] by JRaffo 
*/

/**
* @author  Javier Raffo
* @version 1.0
* @since   26/02/2016 21:33:30
*/

Ext.define('MyApp.controller.GestionarUsuario.MainController', {
    extend: 'Ext.app.Controller',

    stores: ['GestionarUsuario.MainStore'],

    models: ['GestionarUsuario.MainModel'],

    views: ['GestionarUsuario.Edit', 'GestionarUsuario.List'],

    refs: [
    	{
            ref: 'GestionarUsuarioPanel',
            selector: 'panel'
        },
        {
            ref: 'GestionarUsuarioList',
            selector: 'GestionarUsuarioList'
        }
    ],

    init: function() {
    
        Ext.getStore('GestionarUsuario.MainStore').addListener('beforeload',this.setExtraParams,this);
        
        this.control({
            'GestionarUsuarioList dataview': {
                itemdblclick: this.editRecord
            },
            'GestionarUsuarioList button[action=add]': {
            	click: this.editRecord
            },
            'GestionarUsuarioList button[action=delete]': {
                click: this.deleteRecord
            },
            'GestionarUsuarioEdit button[action=save]': {
                click: this.updateRecord
            },
            'GestionarUsuarioList button[action=filter]': {
                click: this.refreshGrid
            },
            'GestionarUsuarioList button[action=clearFilter]': {
                click: this.resetForm
            },
            'GestionarUsuarioList combobox': {
                change: this.refreshGrid
            },
            'GestionarUsuarioList textfield': {
            	specialkey: this.onKeyEnter
            }
        });
    },

    editRecord: function(grid, record) {
        var edit = Ext.create('MyApp.view.GestionarUsuario.Edit').show();
        var form = edit.down('form');
        if(record.data){
        	edit.down('form').loadRecord(record);
        	edit.setTitle('Editar');
        	
        	var subModel_UsuarioPerfilSubCrud = Ext.getCmp('subModel_UsuarioPerfilSubCrud');
        	var id_usuario = form.getForm().findField('id_usuario');
        	
			subModel_UsuarioPerfilSubCrud.store.reload({params: {id_usuario: id_usuario.getValue(), start:0, limit:10}});
			
        }else{
        	edit.setTitle('Agregar');
        	var subModel_UsuarioPerfilSubCrud = Ext.getCmp('subModel_UsuarioPerfilSubCrud');
			subModel_UsuarioPerfilSubCrud.store.reload({params: {id_usuario: -1, start:0, limit:10}});
        }
    },
    
    updateRecord: function(button) {
    	
        var win = button.up('window');
        var form = win.down('form');
        var record = form.getRecord();
        var values = form.getValues();
        var store = Ext.getStore('GestionarUsuario.MainStore');
        
        if (form.isValid()) {
	        Ext.Msg.confirm('Confirmar','¿Estás seguro de GRABAR el registro?',function (btn) {
	            
	            if (btn == 'yes') {
	        		if (values.id_usuario > 0){
	        			values.tokenField = 'update';
	        			record.set(values);
	        		} else{
	        			record = Ext.create('MyApp.model.GestionarUsuario.MainModel');
	        			values.tokenField = 'insert';
	        			record.set(values);
	        			store.add(record);
	        		}
	                
	        		store.sync({
	        	        success: function (batch, eOpts) {
				        	store.load();
				        	win.close();
	        	        },
	        	        failure: function (batch, eOpts) {
							store.rejectChanges();
	        	        }
	        	    });
	            }
	        });
		}

    },
    
    deleteRecord: function(button) {
    	
    	var grid = this.getGestionarUsuarioList();
    	var record = grid.getSelectionModel().getSelection(); 
    	var store = Ext.getStore('GestionarUsuario.MainStore');
    	
    	if(record.length > 0){
	        Ext.Msg.confirm('Confirmar','¿Estás seguro de ELIMINAR el registro?',function (btn) {
	                if (btn == 'yes') {
	             	   store.remove(record);
						store.sync({
					        success: function (batch, eOpts) {
					        	store.load();
					        }
					    });
	                }
	        });
    	}

    },
    refreshGrid: function() {
    	var store = Ext.getStore('GestionarUsuario.MainStore');
    	store.load({start:0});
    },
    resetForm: function() {
    	var store = Ext.getStore('GestionarUsuario.MainStore');
        var form = Ext.getCmp('form_filter_GestionarUsuario');
        form.reset();
    	store.load({start:0});
    },
    onKeyEnter: function(field, event, options) {
        if (event.getKey() == event.ENTER) {
        	var store = Ext.getStore('GestionarUsuario.MainStore');
        	store.load({start:0});
        }
    },
    setExtraParams: function(field, event, options) {
    	var store = Ext.getStore('GestionarUsuario.MainStore');
        var form = Ext.getCmp('form_filter_GestionarUsuario');
        var values = form.getValues();
    	store.getProxy().extraParams = values;
    }
    
});