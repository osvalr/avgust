/*
* Copyright «©» 2016, J.R Totem. All rights reserved.
*
* This file was generated by GeneratorCrud v.1.0.
* All changes made in this file will be lost if you recompile the source schema.
*
* [GeneratorCrud v.1.0] by JRaffo 
*/

/**
* @author  Javier Raffo
* @version 1.0
* @since   01/03/2016 21:35:12
*/

Ext.define('MyApp.controller.GestionarClienteNatural.MainController', {
    extend: 'Ext.app.Controller',

    stores: ['GestionarClienteNatural.MainStore'],

    models: ['GestionarClienteNatural.MainModel'],

    views: ['GestionarClienteNatural.Edit', 'GestionarClienteNatural.List'],

    refs: [
    	{
            ref: 'GestionarClienteNaturalPanel',
            selector: 'panel'
        },
        {
            ref: 'GestionarClienteNaturalList',
            selector: 'GestionarClienteNaturalList'
        }
    ],

    init: function() {
    
        Ext.getStore('GestionarClienteNatural.MainStore').addListener('beforeload',this.setExtraParams,this);
    
        this.control({
            'GestionarClienteNaturalList dataview': {
                itemdblclick: this.editRecord
            },
            'GestionarClienteNaturalList button[action=add]': {
            	click: this.editRecord
            },
            'GestionarClienteNaturalList button[action=delete]': {
                click: this.deleteRecord
            },
            'GestionarClienteNaturalEdit button[action=save]': {
                click: this.updateRecord
            },
            'GestionarClienteNaturalList button[action=filter]': {
                click: this.refreshGrid
            },
            'GestionarClienteNaturalList button[action=clearFilter]': {
                click: this.resetForm
            },
            'GestionarClienteNaturalList combobox': {
                change: this.refreshGrid
            },
            'GestionarClienteNaturalList textfield': {
            	specialkey: this.onKeyEnter
            }
        });
    },

    editRecord: function(grid, record) {
        var edit = Ext.create('MyApp.view.GestionarClienteNatural.Edit').show();
        var form = edit.down('form');
        
        if(record.data){
        	edit.down('form').loadRecord(record);
        	edit.setTitle('Editar');
        }else{
        	edit.setTitle('Agregar');
        	
          	var id_estado = form.getForm().findField('id_estado');
          	id_estado.setReadOnly(true);
          	id_estado.setValue(1);
        	
        }
    },
    
    updateRecord: function(button) {
    	
        var win = button.up('window');
        var form = win.down('form');
        var record = form.getRecord();
        var values = form.getValues();
        var store = Ext.getStore('GestionarClienteNatural.MainStore');
        
        if (form.isValid()) {
	        Ext.Msg.confirm('Confirmar','¿Estás seguro de GRABAR el registro?',function (btn) {
	            
	            if (btn == 'yes') {
	        		if (values.id_persona > 0){
	        			values.tokenField = 'update';
	        			record.set(values);
	        		} else{
	        			record = Ext.create('MyApp.model.GestionarClienteNatural.MainModel');
	        			values.tokenField = 'insert';
	        			record.set(values);
	        			store.add(record);
	        		}
	                	
	        		store.sync({
	        	        success: function (batch, eOpts) {
				        	store.load();
				        	win.close();
	        	        },
	        	        failure: function (batch, eOpts) {
							store.rejectChanges();
	        	        }
	        	    });
	            }
	        });
		}
    },
    
    deleteRecord: function(button) {
    	
    	var grid = this.getGestionarClienteNaturalList();
    	var record = grid.getSelectionModel().getSelection(); 
    	var store = Ext.getStore('GestionarClienteNatural.MainStore');
    	
    	if(record.length > 0){
	        Ext.Msg.confirm('Confirmar','¿Estás seguro de ELIMINAR el registro?',function (btn) {
	                if (btn == 'yes') {
	             	   store.remove(record);
						store.sync({
					        success: function (batch, eOpts) {
					        	store.load();
					        },
		        	        failure: function (batch, eOpts) {
								store.rejectChanges();
		        	        }
					    });
	                }
	        });
    	}

    },
    refreshGrid: function() {
    	var store = Ext.getStore('GestionarClienteNatural.MainStore');
    	store.load({start:0});
    },
    resetForm: function() {
    	var store = Ext.getStore('GestionarClienteNatural.MainStore');
        var form = Ext.getCmp('form_filter_GestionarClienteNatural');
        form.reset();
    	store.load({start:0});
    },
    onKeyEnter: function(field, event, options) {
        if (event.getKey() == event.ENTER) {
        	var store = Ext.getStore('GestionarClienteNatural.MainStore');
        	store.load({start:0});
        }
    },
    setExtraParams: function(field, event, options) {
    	var store = Ext.getStore('GestionarClienteNatural.MainStore');
        var form = Ext.getCmp('form_filter_GestionarClienteNatural');
        var values = form.getValues();
    	store.getProxy().extraParams = values;
    }
});