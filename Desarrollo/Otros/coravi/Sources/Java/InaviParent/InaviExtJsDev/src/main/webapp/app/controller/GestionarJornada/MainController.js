/*
* Copyright «©» 2016, J.R Totem. All rights reserved.
*
* This file was generated by GeneratorCrud v.1.0.
* All changes made in this file will be lost if you recompile the source schema.
*
* [GeneratorCrud v.1.0] by JRaffo 
*/

/**
* @author  Javier Raffo
* @version 1.0
* @since   01/03/2016 01:00:19
*/

Ext.define('MyApp.controller.GestionarJornada.MainController', {
    extend: 'Ext.app.Controller',

    stores: ['GestionarJornada.MainStore'],

    models: ['GestionarJornada.MainModel'],

    views: ['GestionarJornada.Edit', 'GestionarJornada.List'],

    refs: [
    	{
            ref: 'GestionarJornadaPanel',
            selector: 'panel'
        },
        {
            ref: 'GestionarJornadaList',
            selector: 'GestionarJornadaList'
        }
    ],

    init: function() {
    
        Ext.getStore('GestionarJornada.MainStore').addListener('beforeload',this.setExtraParams,this);
    
        this.control({
            'GestionarJornadaList dataview': {
//                itemdblclick: this.editRecord
            },
            'GestionarJornadaList button[action=add]': {
            	click: this.editRecord
            },
            'GestionarJornadaList button[action=delete]': {
                click: this.deleteRecord
            },
            'GestionarJornadaEdit button[action=save]': {
                click: this.updateRecord
            },
            'GestionarJornadaList button[action=filter]': {
                click: this.refreshGrid
            },
            'GestionarJornadaList button[action=clearFilter]': {
                click: this.resetForm
            },
            'GestionarJornadaList combobox': {
                change: this.refreshGrid
            },
            'GestionarJornadaList textfield': {
            	specialkey: this.onKeyEnter
            }
        });
    },

    editRecord: function(grid, record) {
        var edit = Ext.create('MyApp.view.GestionarJornada.Edit').show();
        var form = edit.down('form');
        
        if(record.data){
        	edit.down('form').loadRecord(record);
        	edit.setTitle('Editar');
        }else{
        	edit.setTitle('Agregar');
        	
          	var id_estado = form.getForm().findField('id_estado');
          	id_estado.setReadOnly(true);
          	id_estado.setValue(1);
          	
        }
    },
    
    updateRecord: function(button) {
    	
        var win = button.up('window');
        var form = win.down('form');
        var record = form.getRecord();
        var values = form.getValues();
        var store = Ext.getStore('GestionarJornada.MainStore');
        
        if (form.isValid()) {
	        Ext.Msg.confirm('Confirmar','¿Estás seguro de GRABAR el registro?',function (btn) {
	            
	            if (btn == 'yes') {
	        		if (values.num_anio > 0){
	        			values.tokenField = 'update';
	        			record.set(values);
	        		} else{
	        			record = Ext.create('MyApp.model.GestionarJornada.MainModel');
	        			values.tokenField = 'insert';
	        			record.set(values);
	        			store.add(record);
	        		}
	                	
	        		store.sync({
	        	        success: function (batch, eOpts) {
				        	store.load();
				        	win.close();
	        	        },
	        	        failure: function (batch, eOpts) {
							store.rejectChanges();
	        	        }
	        	    });
	            }
	        });
		}
    },
    
    deleteRecord: function(button) {
    	
    	var grid = this.getGestionarJornadaList();
    	var record = grid.getSelectionModel().getSelection(); 
    	var store = Ext.getStore('GestionarJornada.MainStore');
    	
    	if(record.length > 0){
	        Ext.Msg.confirm('Confirmar','¿Estás seguro de ELIMINAR el registro?',function (btn) {
	                if (btn == 'yes') {
	             	   store.remove(record);
						store.sync({
					        success: function (batch, eOpts) {
					        	store.load();
					        },
		        	        failure: function (batch, eOpts) {
								store.rejectChanges();
		        	        }
					    });
	                }
	        });
    	}

    },
    refreshGrid: function() {
    	var store = Ext.getStore('GestionarJornada.MainStore');
    	store.load({start:0});
    },
    resetForm: function() {
    	var store = Ext.getStore('GestionarJornada.MainStore');
        var form = Ext.getCmp('form_filter_GestionarJornada');
        form.reset();
    	store.load({start:0});
    },
    onKeyEnter: function(field, event, options) {
        if (event.getKey() == event.ENTER) {
        	var store = Ext.getStore('GestionarJornada.MainStore');
        	store.load({start:0});
        }
    },
    setExtraParams: function(field, event, options) {
    	var store = Ext.getStore('GestionarJornada.MainStore');
        var form = Ext.getCmp('form_filter_GestionarJornada');
        var values = form.getValues();
    	store.getProxy().extraParams = values;
    }
});